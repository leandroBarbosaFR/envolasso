import { definePlugin } from 'sanity';
// ===============================================================
// Documents
// ===============================================================
import globalSetings from './schemas/documents/global-settings';
import siteSetings from './schemas/documents/site-setings';
// ===============================================================
// Objects
// ===============================================================

// ===============================================================
// Registration form fields config.
// ===============================================================

const agencySeo = definePlugin(() => ({
  schema: {
    types: [
      // ===============================================================
      // Documents
      // ===============================================================
      globalSetings,
      siteSetings
      // ===============================================================
      // Objects
      // ===============================================================

      // ===============================================================
      // Registration form fields config
      // ===============================================================
    ]
  }
}));

export default agencySeo;
