const FormInputPassword = {
  // ==========================================================================
  // layoutVariant
  // ==========================================================================
  default: {
    // ==========================================================================
    // StyledShowPassword
    // ==========================================================================
    StyledShowPassword: {
      position: 'absolute',
      top: '30px',
      right: '-1px',
      paddingRight: `10px`,
      Button: {
        variant: `buttonIconPrimary`,
        withIcon: true,
        withText: false
      }
    }
  }
};

export { FormInputPassword };
